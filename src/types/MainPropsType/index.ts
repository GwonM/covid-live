export type MainProps = {
    TotalCount: TypeOfTotal;
    incDec: TypeOfInc;
};

export type TypeOfTotal = {
    decideCnt: number;
    deathCnt: number;
};

export type TypeOfInc = {
    decideCnt: number;
    deathCnt: number;
};
